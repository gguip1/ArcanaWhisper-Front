import React from 'react';
import TarotHistory from '../components/TarotHistory';

const HistoryPage: React.FC = () => {
  return <TarotHistory onGoHome={() => {}} />;
};

export default HistoryPage;
